name: user_manager
description: User management API.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^0.18.4+1
  firebase_core: ^0.5.3
  firebase_database: ^4.4.0
  flutter_facebook_auth: ^1.0.2+2
  cloud_firestore: ^0.14.4
  shared_preferences: '>=0.5.12+4 <2.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.14.4
  mockito: ^4.1.3
  pedantic: ^1.9.0
  cloud_firestore_mocks: ^0.5.2+1
  firebase_database_mocks:
    git:
      url: https://github.com/sitatec/firebase_database_mocks.git
      ref: main
